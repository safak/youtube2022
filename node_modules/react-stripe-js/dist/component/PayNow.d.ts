/// <reference types="react" />
import { Stripe } from "@stripe/stripe-js";
interface iPayNow {
    title?: string;
    successMessage?: string;
    stripe: Promise<Stripe | null>;
    clientSecret: string;
    onClick: () => void;
    onPaymentSuccess: () => void;
}
export default function PayNow({ title, stripe, clientSecret, onClick, onPaymentSuccess, successMessage }: iPayNow): JSX.Element;
export {};
